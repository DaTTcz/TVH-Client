# vendor/mpv

Put `mpv.lib` here (the linker looks for it via `.cargo/config.toml`).

See the main README.md, section "Video playback (mpv) - Windows setup",
for how to get it from an mpv Windows build.
